// Generated by CoffeeScript 1.10.0
(function() {
  var BaseModel, OAuth,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  BaseModel = require('../BaseModel');

  OAuth = (function(superClass) {
    extend(OAuth, superClass);

    function OAuth() {
      this.clean = bind(this.clean, this);
      this.authorize = bind(this.authorize, this);
      this.init = bind(this.init, this);
      return OAuth.__super__.constructor.apply(this, arguments);
    }

    OAuth.prototype.init = function() {
      this.debug("init()");
      this.program.command("authorize").alias("auth").action(this.authorize);
      return this.program.command("clean").action(this.clean);
    };

    OAuth.prototype.authorize = function() {
      this.debug("login()");
      return this.coding.oauth.authorize(this.opts.clientId, this.opts.clientSecret, this.opts.port, this.opts.scope, (function(_this) {
        return function(data) {
          return _this.showData(data);
        };
      })(this));
    };

    OAuth.prototype.clean = function() {
      this.debug("logout()");
      return this.coding.oauth.clean;
    };

    return OAuth;

  })(BaseModel);

  module.exports = function(cmd) {
    return new OAuth(cmd);
  };

}).call(this);
